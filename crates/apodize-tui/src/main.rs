//! TUI frontend scaffold.
//!
//! The interactive TUI is intentionally deferred until the instance Core API and persistence
//! boundary have stabilized. Keeping this crate in the workspace prevents the CLI from becoming
//! the accidental owner of application logic while avoiding a premature terminal UI dependency.

fn main() {
    println!("Apodize TUI is not implemented yet.");
}
